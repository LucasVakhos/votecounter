# STAGE33 - DevExpress Side Navigation + Frame

Что изменено:

- верхний `XtraTabControl` главного окна заменён на каркас:
  - слева боковая DevExpress-панель навигации;
  - справа `NavigationFrame` с рабочими страницами.
- основные разделы теперь переключаются через левое меню:
  - Конкурсы;
  - Голосование;
  - Настройки конкурса;
  - Контроль голосования;
  - Итоги;
  - Правки правил;
  - Авторы.
- внутренние маленькие вкладки предпросмотра на первой странице оставлены, потому что там они работают как локальный переключатель карточки конкурса.
- переходы из кнопок и popup-меню переведены с индексов вкладок на метод `NavigateToSection(...)`.
- layout в `Setting.Ini` теперь сохраняет:
  - выбранный раздел боковой панели;
  - ширину боковой панели;
  - прежние layout таблиц и выбранный конкурс.

База остаётся здесь:

```text
VoteCounter/Database/VoteCounter.db
```

Рабочая база не входит в ZIP, чтобы не затирать данные при распаковке поверх проекта.
