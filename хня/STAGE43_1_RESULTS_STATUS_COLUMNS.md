# Stage 43.1 - статусы в итоговой таблице

Добавлены явные статусы в таблицу итогов конкурса.

Что сделано:
- в модель итоговой строки добавлены `AcceptedText` и `VoteStatusText`;
- на вкладке `Итоги` появились колонки `Принято` и `Голос есть`;
- расчёт мест, баллов, средних, самоголосов и максимальных оценок не менялся;
- версия проекта поднята до `1.43.1-results-status-columns`.

Проверка:
- `dotnet build --no-restore -p:RestorePackagesPath="C:\Users\User\.nuget\packages" -p:NuGetAudit=false`
- результат: 0 ошибок, 0 предупреждений.
