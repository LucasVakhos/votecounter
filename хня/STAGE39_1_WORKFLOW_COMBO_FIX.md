# STAGE39.1 - Исправление списка текущего конкурса

Что исправлено:

- верхний список `Текущий конкурс` больше не привязан напрямую к объектам `Contest`;
- для комбобокса добавлены простые строки отображения `№ - Название (статус)`;
- выбор из верхнего списка снова устанавливает текущий конкурс по `Id`;
- активный конкурс в списке поднимается первым;
- в тексте строки видно, активный это конкурс или архивный.

Проверка:

- `dotnet build --no-restore -p:RestorePackagesPath="C:\Users\User\.nuget\packages" -p:NuGetAudit=false`
- результат: 0 ошибок, 0 предупреждений.
