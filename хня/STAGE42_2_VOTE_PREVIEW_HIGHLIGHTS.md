# Stage 42.2 - подсветка авточека голосов

Добавлена визуальная подсветка строк в таблице голосов.

Что сделано:
- в таблицу `Данные конкурса` добавлена колонка `Статус`;
- обычные принятые строки подсвечиваются зелёным;
- строки, исправленные правилами, подсвечиваются жёлтым;
- самоголосование подсвечивается красным;
- подсветка работает и для предпросмотра, и для загруженных голосов из базы;
- логика статуса собрана в `GridRow`, отрисовка в `PaintVoteRow`.

Проверка:
- `dotnet build --no-restore -p:RestorePackagesPath="C:\Users\User\.nuget\packages" -p:NuGetAudit=false`
- результат: 0 ошибок, 0 предупреждений.
