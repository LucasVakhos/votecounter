# Stage 43.2 - команды итогового протокола

Приведены команды вкладки `Итоги` к рабочему сценарию Stage 43.

Что сделано:
- кнопка `Обновить итоги` переименована в `Подвести итоги`;
- команда `Подвести итоги` теперь переводит конкурс в стадию `4 - Итоги` и пересчитывает таблицу;
- кнопка Excel переименована в `Экспорт в Excel`;
- кнопка HTML переименована в `Сохранить HTML`;
- такие же названия применены в popup-меню итоговой таблицы;
- версия проекта поднята до `1.43.2-results-actions`.

Проверка:
- `dotnet build --no-restore -p:RestorePackagesPath="C:\Users\User\.nuget\packages" -p:NuGetAudit=false`
- результат: 0 ошибок, 0 предупреждений.
