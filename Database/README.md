# Database

Рабочая база создаётся приложением здесь:

```text
VoteCounter/Database/VoteCounter.db
```

Файл `VoteCounter.db` намеренно не кладётся в ZIP-чекпойнты, чтобы новая распаковка проекта не затирала ваши конкурсы.

В ZIP должны попадать только этот README и `.gitkeep`.
